spring:
  application:
    name: ieum-backend
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

cloud:
  aws:
    region:
      static: ap-northeast-2
    s3:
      bucket: onsaem-bucket